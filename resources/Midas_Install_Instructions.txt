Instructions from top to bottom for a fresh Midas_wx\Midas_kv installation of Midas in pycharm on Windows.

0. Clone repo from https://github.com/ilvanic-11/MIDAS/tree/Isaacs_Mayavi_Branch
	#TODO-- figure out exact git clone methodology
	git clone -b Isaacs_Mayavi_Branch https://github.com/ilvanic-11/MIDAS/tree/
	OR
	Direct download from Isaacs_Mayavi_Branch on git and manual install to a specified directory.

1. 
	Download and Install Pycharm, Notepad++, Anaconda (if you wish), Microsoft Visual C++ Redistributable 
	-Google:
		-->pycharm download
		-->notepad++ download
		-->Run this in web browser url bar -->  https://aka.ms/vs/17/release/vc_redist.x64.exe --This will immediately download what is needed.
		-->https://docs.microsoft.com/en-US/cpp/windows/latest-supported-vc-redist?view=msvc-170
		-->(for demonstration purposes, it is advised to install FL Studio20+, any version(Fruity, Producer, etc.) that uses the Piano Roll)
		-->anaconda download(if you wish
		-
	
	
*
2. Install Python 3.7.4
	-Add python to PATH
	-allow Path limit max
	-customize as needed regarding your pycharm\python preferences\setup
	
2. IN PYCHARM, create a NEW virtual environment(venv).
	-enable Inherit from global.
	-File>Settings>Project>Python Interpreter>*Cogwheel - Showall>Add +Plus sign  (Not most recent Pycharm version...)
	-File>Settings>Project>Python Interpreter>*Dropdown Arrow - Showall>Add +Plus sign   (Most recent)
	-Check "New Environment
		Location - Your project folder you created and established at the point of the git clone step above.
		Base Interpreter -- Navigate to you Python3.7.4. executable.
	Click Okay and Apply.
	(Feel free to restart pycharm for refresh)
4. IN PYCHARM, click "Terminal" at the bottom.
	In terminal window toolbar, click the dropdown arrow at the far right and select Command Prompt.
	The terminal should display the following:
	(venv) C:\Users\user\Midas>  (Make sure it is the \Midas folder.)
	
	Now, execute -->
	git clone https://github.com/openai/point-e
	cd point-e  (The pip install requirement '-e .' can only be executed from directory \point-e.)
	
	##MIDAS_kivy
	#####
	(Necessary for Python 3.11.5)

	Then, execute:
	--> pip install attrdict, requests, matplotlib
	--> pip install numpy==1.21
	

	python -m pip install opencv-python==4.5.1.48
	Locate the mapping.py, mixins.py, merge.py, default.py files within the attrdict package in your site-packages folder.
	Change the line from collections import Mapping to from collections.abc import Mapping
	(vtk==9.2.4 for Python3.11)
	#####



	##MIDAS_kivy
	include this is main.spec
	#####################
	block_cipher = None
	from kivy_deps import sdl2, glew
	from kivymd import hooks_path as kivymd_hooks_path
	import pkg_resources.py2_warn

	a = Analysis(
		['main.py'],
		pathex=["C:\Users\MIDAS_kivy\venv\Lib\site-packages"],
		binaries=[],
		datas=[],
		hiddenimports=['kivy', 'pkg_resources.py2_warn'],
		hookspath=[],
		hooksconfig={},
		runtime_hooks=[],
		excludes=[],
		win_no_prefer_redirects=False,
		win_private_assemblies=False,
		cipher=block_cipher,
		noarchive=False,
	#####################
	


	From here, execute the following command: pip install -r C:\Users\youtheuser\Midas\resources\Midas_Requirements.txt

	##MIDAS_kivy
	pyinstaller --paths "C:\Users\MIDAS_kivy\venv\Lib\site-packages" C:\Users\MIDAS_kivy\main.py
	
	
5.(Optional)) Install CUDA. Download and install CUDA from their website. Note: Requires about 12gb of free disk space.
	(Installation of CUDA is not necessary for Midas as whole, but there is one feature that is significantly faster user it.)
	IN PYCHARM, b. In the Settings/Preferences dialog, navigate to "Project" > "Python Interpreter".

				c. Select the Python interpreter you are using for your project.

				d. Click on the "Show All" button or the gear icon, and then select "Edit".

				e. In the "Environment Variables" section, click on the "+" button to add a new environment variable.

				f. Set the name of the variable to PYCHARM_CUDA and set its value to 1.

				g. Click "OK" to save the changes.
				
	IN CMD PROMPT
				h. python -m pip install torch==1.13.1+cu117 torchvision>=0.13.1+cu117 torchaudio>=0.13.1+cu117 --extra-index-url https://download.pytorch.org/whl/cu117 --no-cache-dir --user

	Restart PyCharm: Close and reopen PyCharm to ensure the changes take effect.

				i. Restart PyCharm.
				
	Run MIDAS_wx.
	Run MIDAS_kivy.
	
	
	
MIDAS_vst
Overwrite installation DATA folder with --> https://github.com/kunitoki/JUCE/tree/99112cf71f0cc3aafa4622b62cf81f2b7df38f0b/modules this.
Issue solution found here. --> https://github.com/kunitoki/popsicle/issues/3
#TODO Return to later. IN alpha version, not ready for development usage yet, for my purposes.
